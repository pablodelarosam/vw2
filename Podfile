# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'YoSoyVW' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for YoSoyVW
    pod 'RxSwift', '~> 5'
    pod 'RxCocoa', '~> 5'
    pod 'AWSMobileClient', '~> 2.9.0'        # Required dependency
    pod 'AWSAuthUI', '~> 2.9.0'            # Optional dependency required to use drop-in 
    pod 'AWSUserPoolsSignIn', '~> 2.9.0'   # Optional dependency required to use drop-in
    pod 'AWSAPIGateway'
 

  target 'YoSoyVWTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'YoSoyVWUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
